import { Telegraf } from "telegraf";
import { bot } from ".";

// bot.start((ctx) => {
//   ctx.reply("Welcome! Use /add_webpage to add a webpage.");
// });

// bot.command("register_schedule", (ctx) => {
//   ctx.reply(
//     "Please provide the URL of the webpage you want to add." + ctx.chat.id
//   );
// });

// bot.command("update_schedule", (ctx) => {
//   ctx.reply(
//     "Please provide the URL of the webpage you want to add." + ctx.chat.id
//   );
// });

// } catch (error) {

// }

// bot.launch();
